- hosts: proxmox
  become: false
  gather_facts: false
  tasks:
  - name: Just simply testing iptables.
    ansible.builtin.iptables:
      chain: INPUT
        protocol: tcp
        destination_port: 22
        jump: ACCEPT
      become: yes
